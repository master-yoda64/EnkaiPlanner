# config.yaml
defaults:  
  - override hydra/hydra_logging: disabled  
  - override hydra/job_logging: disabled

hydra:
  run:
    dir: "."  # 出力ディレクトリを無効にする

data:
  xlsx_path: "../data/sensyn.xlsx"
  table_num: 9
  job_field_str_list: ["group1", "group2", "group3", "group4", "group5"]
  job_class_str: "job_class"

logger:
  console_debug: False
